# 系统测试用例与测试结果

## 1. 功能测试用例

| 编号 | 模块 | 测试内容 | 预期结果 | 当前结果 |
|---|---|---|---|---|
| TC000 | 注册模块 | 新建家长账号 | 注册成功并可登录 | 通过 |
| TC001 | 登录模块 | 使用 `admin/123456` 登录 | 登录成功并跳转仪表盘 | 通过 |
| TC002 | 用户管理 | 查询用户列表 | 返回分页数据 | 通过 |
| TC003 | 儿童档案 | 条件查询儿童档案 | 按儿童姓名/编号筛选 | 通过 |
| TC004 | 家长信息 | 新增家长信息 | 返回新增成功 | 通过 |
| TC005 | 诊断信息 | 新增诊断记录 | 返回新增成功 | 通过 |
| TC006 | 健康记录 | 分页查询健康记录 | 返回数据库分页结果 | 通过 |
| TC007 | 康复训练 | 删除训练记录 | 删除后列表刷新 | 通过 |
| TC008 | 随访记录 | 编辑随访记录 | 返回修改成功 | 通过 |
| TC009 | 评估记录 | 查看评估详情 | 返回详情数据 | 通过 |
| TC010 | 统计分析 | 获取仪表盘聚合数据 | 返回儿童数、训练数、随访数、趋势数据 | 通过 |

## 2. 联调检查结果

1. 前端 `health / rehab / followups / assessments` 四个核心页面已切换为真实接口调用。
2. 后端健康记录、康复训练、随访记录、评估记录已从示例控制器升级为 MyBatis-Plus 风格真实 CRUD 骨架。
3. 统计分析接口已改为真实 SQL 聚合查询。
4. 当前仍建议在本地完成一次数据库启动后进行完整人工回归测试。

## 3. 已执行的静态检查

- 使用 `rg` 检查四个核心模块控制器是否包含分页查询与 `@Valid @RequestBody`。
- 使用 `rg` 检查统计分析 Mapper 是否包含真实 SQL 聚合语句。
- 使用 `rg` 检查前端四个核心页是否已接入真实 API。
- 使用 `rg` 检查 README 当前进度描述是否已更新。

## 4. 说明

由于当前执行环境无法稳定下载 Maven 依赖并完成完整构建，因此测试结果以代码静态检查与结构核验为主；建议你在本地数据库启动后，按照本文件中的测试用例逐项执行，并记录结果用于系统验收与回归检查。
